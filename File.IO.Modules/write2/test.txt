ในโค้ดด้านบนopen()ฟังก์ชันจะเปิดไฟล์ข้อความในโหมดอ่าน ทำให้เราดึงข้อมูลจากไฟล์ได้โดย
ไม่เปลี่ยนแปลงข้อมูลโดยไม่ได้ตั้งใจ ในบรรทัดแรก เอาต์พุตของopen()ฟังก์ชันจะถูกกำหนดให้
กับfตัวแปร ซึ่งเป็นอ็อบเจ็กต์ที่แสดงถึงไฟล์ข้อความ ในบรรทัดที่สองของโค้ดด้านบน เราใช้
read()เมธอดเพื่ออ่านไฟล์ทั้งหมดและพิมพ์เนื้อหาclose()Thank for